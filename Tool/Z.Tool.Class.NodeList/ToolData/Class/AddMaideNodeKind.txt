    maide precate #ItemClassName# AddItem(var String name, var Node node, var NodeState nodeState, var NewState newState, var SetState setState)
    {
        node.Init();
        nodeState.Init();
        newState.Init();
        setState.Init();

        var #ItemClassName# item;
        item : new #ItemClassName#;
        item.Init();
        item.Index : this.Index;
        item.Name : name;
        item.Node : node;
        item.NodeState : nodeState;
        item.NewState : newState;
        item.SetState : setState;
        this.Array.Set(item.Index, item);
        this.Index : this.Index + 1;
        return item;
    }
